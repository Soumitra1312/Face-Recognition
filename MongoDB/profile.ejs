<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-blue-100 to-indigo-200 flex items-center justify-center p-10">
    <div class="bg-white shadow-2xl rounded-2xl p-10 w-full max-w-4xl text-black">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-3xl font-bold">Welcome, <span class="text-indigo-600"><%= user.name %></span></h2>
            <a class="bg-red-500 hover:bg-red-600 text-white rounded-md px-4 py-2 text-sm transition" href="/logout">Logout</a>
        </div>

        <p class="text-lg text-gray-700 mb-6">You can start taking attendance by describing the classroom below.</p>

        <form method="post" action="/post" class="mb-12">
            <!-- Classroom Description -->
            <textarea 
                placeholder="Enter Classroom Description..." 
                name="content" 
                class="w-full md:w-2/3 h-28 p-4 border border-zinc-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-400 resize-none mb-4"
            ></textarea>
            
            <!-- Section Field -->
            <input 
                type="text" 
                name="section" 
                placeholder="Enter Section" 
                class="w-full md:w-2/3 p-4 border border-zinc-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-400 mb-4"
            >
        
            <!-- Subject Field -->
            <input 
                type="text" 
                name="subject" 
                placeholder="Enter Subject" 
                class="w-full md:w-2/3 p-4 border border-zinc-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-400 mb-4"
            >
        
            <!-- Periods Field -->
            <input 
                type="number" 
                name="periods" 
                placeholder="Enter Number of Periods" 
                class="w-full md:w-2/3 p-4 border border-zinc-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-400 mb-4"
            >
        
            <!-- Room Number Field -->
            <input 
                type="text" 
                name="roomNumber" 
                placeholder="Enter Room Number" 
                class="w-full md:w-2/3 p-4 border border-zinc-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-400 mb-4"
            >
            
            <!-- Submit Button -->
            <input 
                class="bg-indigo-500 hover:bg-indigo-600 text-white px-6 py-2 rounded-md transition cursor-pointer" 
                type="submit" 
                value="Submit"
            >
        </form>
        

        <div class="posts">
            <h3 class="text-2xl font-semibold text-zinc-900 mb-4">Your Classroom Descriptions</h3>
            <div class="postcontainer grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% user.posts.reverse().forEach(function(post){ 
                    const postDate = new Date(post.date);
                    const formattedDate = postDate.toLocaleDateString("en-IN", { year: 'numeric', month: 'short', day: 'numeric' });
                    const formattedTime = postDate.toLocaleTimeString("en-IN", { timeZone: "Asia/Kolkata", hour: '2-digit', minute: '2-digit', hour12: true });
                %>
                    <div class="post bg-white p-4 border border-zinc-300 rounded-xl shadow-sm">
                        <h4 class="text-blue-600 font-medium mb-2">Mr. <%= user.username %></h4>
                        <p class="text-sm text-gray-800 mb-3"><%= post.content %></p>
        
                        <!-- Displaying Section, Subject, Periods, and Room Number -->
                        
        
                        <div class="text-xs text-gray-500 mb-3">
                            📅 <%= formattedDate %><br>
                            🕒 <%= formattedTime %>
                        </div>
        
                        <div class="btns text-sm text-gray-600 space-y-1">
                            <p>Total Students: <span class="font-semibold">--</span></p>
                            <p>Total Present: <span class="font-semibold">--</span></p>
                        </div>
                        <br>
                <a class="bg-blue-500 hover:bg-blue-600 text-white rounded-md px-4 py-2 text-sm transition" href="http://127.0.0.1:5000/" target="">Start Attendance</a>
                    </div>
                <% }) %>
            </div>
        </div>
        
             
    </div>
</body>

</html>